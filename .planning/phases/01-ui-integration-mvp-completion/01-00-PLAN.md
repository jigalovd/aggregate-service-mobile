---
phase: 01-ui-integration-mvp-completion
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified: []
autonomous: false
requirements:
  - BOOK-06
  - BOOK-08
  - BOOK-09
  - REV-04
must_haves:
  truths:
    - "All ScreenModel tests exist for booking and reviews features"
  artifacts:
    - path: "feature/booking/src/commonTest/.../BookingHistoryScreenModelTest.kt"
      provides: "BookingHistoryScreenModel tests"
    - path: "feature/booking/src/commonTest/.../SelectServiceScreenModelTest.kt"
      provides: "SelectServiceScreenModel tests"
    - path: "feature/booking/src/commonTest/.../BookingConfirmationScreenModelTest.kt"
      provides: "BookingConfirmationScreenModel tests"
    - path: "feature/reviews/src/commonTest/.../ReviewsScreenModelTest.kt"
      provides: "ReviewsScreenModel tests"
---

<objective>
Create Wave 0 test files identified as missing in research. These tests are required before executing Wave 1 and Wave 2 plans.
</objective>

<context>
**Wave 0 gaps identified in research:**
- `feature/booking/src/commonTest/.../BookingHistoryScreenModelTest.kt` — covers BOOK-09
- `feature/booking/src/commonTest/.../SelectServiceScreenModelTest.kt` — covers BOOK-06
- `feature/booking/src/commonTest/.../BookingConfirmationScreenModelTest.kt` — covers BOOK-08
- `feature/reviews/src/commonTest/.../ReviewsScreenModelTest.kt` — covers REV-04

**Reference pattern from existing test:**
`feature/catalog/src/commonTest/kotlin/com/aggregateservice/feature/catalog/presentation/screenmodel/CatalogScreenModelTest.kt`

Tests use:
- `kotlin.test` framework
- `StandardTestDispatcher` for coroutine testing
- Functional mocks via anonymous objects implementing repository interfaces
- `@OptIn(ExperimentalCoroutinesApi::class)` annotation
- Pattern: `behavior` lambdas to set mock responses, `runTest` for test body, `testDispatcher.scheduler.advanceUntilIdle()` to flush coroutines
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BookingHistoryScreenModelTest.kt</name>
  <files>
    - feature/booking/src/commonTest/kotlin/com/aggregateservice/feature/booking/presentation/screenmodel/BookingHistoryScreenModelTest.kt
  </files>
  <read_first>
    - feature/booking/src/commonMain/kotlin/com/aggregateservice/feature/booking/presentation/screenmodel/BookingHistoryScreenModel.kt
    - feature/booking/src/commonMain/kotlin/com/aggregateservice/feature/booking/domain/model/Booking.kt
    - feature/catalog/src/commonTest/kotlin/com/aggregateservice/feature/catalog/presentation/screenmodel/CatalogScreenModelTest.kt
  </read_first>
  <action>
    Create `BookingHistoryScreenModelTest.kt` in `feature/booking/src/commonTest/kotlin/com/aggregateservice/feature/booking/presentation/screenmodel/`.

    Follow the pattern from `CatalogScreenModelTest.kt`:
    1. Create functional mock for `BookingRepository`
    2. Set behavior lambdas for `getBookings(clientId)` returning `Result<List<Booking>>`
    3. Test `uiState` initial state
    4. Test loading bookings for a client
    5. Test error handling when fetch fails
    6. Test that `clientId` is passed correctly to repository

    BookingRepository likely has:
    ```kotlin
    suspend fun getBookings(clientId: String): Result<List<Booking>>
    ```

    Booking model likely has:
    - `id: String`
    - `clientId: String`
    - `providerId: String`
    - `providerName: String`
    - `status: BookingStatus`
    - `bookingDate: Instant`
    - `services: List<BookingService>`
    - etc.
  </action>
  <acceptance_criteria>
    - File exists at correct path
    - File compiles without errors
    - At least 3 test cases covering: initial state, successful load, error handling
  </acceptance_criteria>
  <verify>
    <automated>./gradlew :feature:booking:test 2>&1 | grep -E "(error|BUILD|BookingHistoryScreenModelTest|tests)"</automated>
  </verify>
  <done>BookingHistoryScreenModelTest.kt created with basic test coverage</done>
</task>

<task type="auto">
  <name>Task 2: Create SelectServiceScreenModelTest.kt</name>
  <files>
    - feature/booking/src/commonTest/kotlin/com/aggregateservice/feature/booking/presentation/screenmodel/SelectServiceScreenModelTest.kt
  </files>
  <read_first>
    - feature/booking/src/commonMain/kotlin/com/aggregateservice/feature/booking/presentation/screenmodel/SelectServiceScreenModel.kt
    - feature/booking/src/commonMain/kotlin/com/aggregateservice/feature/booking/domain/model/BookingService.kt
    - feature/catalog/src/commonTest/kotlin/com/aggregateservice/feature/catalog/presentation/screenmodel/CatalogScreenModelTest.kt
  </read_first>
  <action>
    Create `SelectServiceScreenModelTest.kt` in `feature/booking/src/commonTest/kotlin/com/aggregateservice/feature/booking/presentation/screenmodel/`.

    Follow the pattern from `CatalogScreenModelTest.kt`:
    1. Create functional mock for `BookingRepository`
    2. Set behavior lambdas for `getProviderServices(providerId)` returning `Result<List<BookingService>>`
    3. Test `initialize(providerId)` loads services
    4. Test service selection (toggle)
    5. Test that selected service IDs are tracked
    6. Test error handling

    The ScreenModel should have:
    - `initialize(providerId: String)` - loads services for provider
    - `uiState: StateFlow<SelectServiceUiState>` with services list and selected IDs
    - `onServiceToggle(serviceId: String)` - toggles selection
  </action>
  <acceptance_criteria>
    - File exists at correct path
    - File compiles without errors
    - At least 3 test cases covering: init loads services, service toggle, error handling
  </acceptance_criteria>
  <verify>
    <automated>./gradlew :feature:booking:test 2>&1 | grep -E "(error|BUILD|SelectServiceScreenModelTest|tests)"</automated>
  </verify>
  <done>SelectServiceScreenModelTest.kt created with basic test coverage</done>
</task>

<task type="auto">
  <name>Task 3: Create BookingConfirmationScreenModelTest.kt</name>
  <files>
    - feature/booking/src/commonTest/kotlin/com/aggregateservice/feature/booking/presentation/screenmodel/BookingConfirmationScreenModelTest.kt
  </files>
  <read_first>
    - feature/booking/src/commonMain/kotlin/com/aggregateservice/feature/booking/presentation/screenmodel/BookingConfirmationScreenModel.kt
    - feature/booking/src/commonMain/kotlin/com/aggregateservice/feature/booking/domain/model/Booking.kt
    - feature/catalog/src/commonTest/kotlin/com/aggregateservice/feature/catalog/presentation/screenmodel/CatalogScreenModelTest.kt
  </read_first>
  <action>
    Create `BookingConfirmationScreenModelTest.kt` in `feature/booking/src/commonTest/kotlin/com/aggregateservice/feature/booking/presentation/screenmodel/`.

    Follow the pattern from `CatalogScreenModelTest.kt`:
    1. Create functional mock for `BookingRepository`
    2. Set behavior lambdas for `getServices(serviceIds)` and `createBooking()`
    3. Test `initialize(...)` loads service data
    4. Test `confirmBooking()` calls repository and updates state
    5. Test error handling

    The ScreenModel likely has:
    - `initialize(providerId, serviceIds, date, time)` - loads service details
    - `uiState: StateFlow<BookingConfirmationUiState>` with services and booking state
    - `confirmBooking()` - creates booking via repository
    - `cancelBooking()` - cancels the flow
  </action>
  <acceptance_criteria>
    - File exists at correct path
    - File compiles without errors
    - At least 3 test cases covering: init loads data, confirm booking success, error handling
  </acceptance_criteria>
  <verify>
    <automated>./gradlew :feature:booking:test 2>&1 | grep -E "(error|BUILD|BookingConfirmationScreenModelTest|tests)"</automated>
  </verify>
  <done>BookingConfirmationScreenModelTest.kt created with basic test coverage</done>
</task>

<task type="auto">
  <name>Task 4: Create ReviewsScreenModelTest.kt</name>
  <files>
    - feature/reviews/src/commonTest/kotlin/com/aggregateservice/feature/reviews/presentation/screenmodel/ReviewsScreenModelTest.kt
  </files>
  <read_first>
    - feature/reviews/src/commonMain/kotlin/com/aggregateservice/feature/reviews/presentation/screenmodel/ReviewsScreenModel.kt
    - feature/reviews/src/commonMain/kotlin/com/aggregateservice/feature/reviews/domain/model/Review.kt
    - feature/catalog/src/commonTest/kotlin/com/aggregateservice/feature/catalog/presentation/screenmodel/CatalogScreenModelTest.kt
  </read_first>
  <action>
    Create `ReviewsScreenModelTest.kt` in `feature/reviews/src/commonTest/kotlin/com/aggregateservice/feature/reviews/presentation/screenmodel/`.

    Follow the pattern from `CatalogScreenModelTest.kt`:
    1. Create functional mock for `ReviewsRepository`
    2. Set behavior lambdas for `getReviews(providerId)` returning `Result<List<Review>>`
    3. Test `initialize(providerId)` loads reviews
    4. Test reviews are displayed correctly
    5. Test `submitReview(rating, comment)` calls repository
    6. Test error handling

    The ScreenModel should have:
    - `initialize(providerId: String)` - loads reviews for provider
    - `uiState: StateFlow<ReviewsUiState>` with reviews list
    - `submitReview(rating: Int, comment: String)` - submits new review
  </action>
  <acceptance_criteria>
    - File exists at correct path
    - File compiles without errors
    - At least 3 test cases covering: init loads reviews, submit review, error handling
  </acceptance_criteria>
  <verify>
    <automated>./gradlew :feature:reviews:test 2>&1 | grep -E "(error|BUILD|ReviewsScreenModelTest|tests)"</automated>
  </verify>
  <done>ReviewsScreenModelTest.kt created with basic test coverage</done>
</task>

</tasks>

<verification>
- ./gradlew :feature:booking:test passes (ensures tests compile AND execute)
- ./gradlew :feature:reviews:test passes (ensures tests compile AND execute)
</verification>

<success_criteria>
1. All 4 missing test files created
2. All test files compile without errors
3. Each test file has at least 3 test cases
4. All tests execute successfully (not just compile)
</success_criteria>

<output>
After completion, update 01-VALIDATION.md wave_0_complete to true