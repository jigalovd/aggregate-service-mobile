---
phase: 01-ui-integration-mvp-completion
plan: "03"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - feature/catalog/src/commonMain/kotlin/com/aggregateservice/feature/catalog/presentation/screen/CatalogScreen.kt
  - feature/catalog/src/commonMain/kotlin/com/aggregateservice/feature/catalog/presentation/screen/SearchScreen.kt
  - feature/catalog/src/commonMain/kotlin/com/aggregateservice/feature/catalog/presentation/screen/ProviderDetailScreen.kt
  - feature/catalog/src/commonMain/kotlin/com/aggregateservice/feature/catalog/presentation/screen/CategorySelectionScreen.kt
autonomous: true
requirements:
  - CAT-04
  - CAT-05
  - CAT-06
  - CAT-07
  - CAT-08
must_haves:
  truths:
    - "CatalogScreen displays providers from real API"
    - "SearchScreen performs debounced search"
    - "ProviderDetailScreen shows provider with services"
    - "CategorySelectionScreen allows category filtering"
  artifacts:
    - path: "feature/catalog/src/commonMain/kotlin/com/aggregateservice/feature/catalog/presentation/screen/CatalogScreen.kt"
      provides: "Provider list with ProviderCard components"
    - path: "feature/catalog/src/commonMain/kotlin/com/aggregateservice/feature/catalog/presentation/screen/SearchScreen.kt"
      provides: "Debounced search functionality"
    - path: "feature/catalog/src/commonMain/kotlin/com/aggregateservice/feature/catalog/presentation/screen/ProviderDetailScreen.kt"
      provides: "Provider details with services list"
---

<objective>
Wire all catalog screens to navigation and connect them to real data via their ScreenModels. ProviderCard component (CAT-08) is used throughout for displaying providers in lists.
</objective>

<context>
@feature/catalog/src/commonMain/kotlin/com/aggregateservice/feature/catalog/presentation/screen/CatalogScreen.kt
@feature/catalog/src/commonMain/kotlin/com/aggregateservice/feature/catalog/presentation/screen/SearchScreen.kt
@feature/catalog/src/commonMain/kotlin/com/aggregateservice/feature/catalog/presentation/screen/ProviderDetailScreen.kt
@feature/catalog/src/commonMain/kotlin/com/aggregateservice/feature/catalog/presentation/screen/CategorySelectionScreen.kt
@feature/catalog/src/commonMain/kotlin/com/aggregateservice/feature/catalog/presentation/component/ProviderCard.kt

**CatalogScreen (CAT-04):**
- Needs to use `koinScreenModel<CatalogScreenModel>()` to get state
- Uses ProviderCard in LazyColumn for displaying providers
- ProviderCard exists at `feature/catalog/presentation/component/ProviderCard.kt`

**SearchScreen (CAT-05):**
- Already has SearchScreenModel with 300ms debounce
- Needs to display search results using ProviderCard

**ProviderDetailScreen (CAT-06):**
- Already has ProviderDetailScreenModel with IsFavoriteUseCase (fixed in Plan 02)
- Needs navigation from catalog list to provider detail

**CategorySelectionScreen (CAT-07):**
- Allows selecting a category to filter providers
- Should navigate back with selected category
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire CatalogScreen to use ScreenModel and ProviderCard</name>
  <files>
    - feature/catalog/src/commonMain/kotlin/com/aggregateservice/feature/catalog/presentation/screen/CatalogScreen.kt
  </files>
  <read_first>
    - feature/catalog/src/commonMain/kotlin/com/aggregateservice/feature/catalog/presentation/screen/CatalogScreen.kt
    - feature/catalog/src/commonMain/kotlin/com/aggregateservice/feature/catalog/presentation/component/ProviderCard.kt
    - feature/catalog/src/commonMain/kotlin/com/aggregateservice/feature/catalog/presentation/screenmodel/CatalogScreenModel.kt
  </read_first>
  <action>
    CatalogScreen needs to be connected to its ScreenModel and display providers using ProviderCard.

    Current CatalogScreen is a `Screen` object. It should:
    1. Get the ScreenModel via `koinScreenModel<CatalogScreenModel>()`
    2. Collect uiState via `screenModel.uiState.collectAsState()`
    3. Display providers in LazyColumn using ProviderCard component
    4. Handle loading, error, and empty states

    Add import for ProviderCard:
    `import com.aggregateservice.feature.catalog.presentation.component.ProviderCard`

    When user taps a ProviderCard, navigate to ProviderDetailScreen with the provider ID.

    Navigation via Voyager:
    ```kotlin
    val navigator = LocalNavigator.currentOrThrow
    // onProviderClick = { provider -> navigator.push(ProviderDetailScreen(provider.id)) }
    ```
  </action>
  <acceptance_criteria>
    - `grep -n "koinScreenModel<CatalogScreenModel>" feature/catalog/src/commonMain/kotlin/com/aggregateservice/feature/catalog/presentation/screen/CatalogScreen.kt` returns at least 1 match
    - `grep -n "ProviderCard" feature/catalog/src/commonMain/kotlin/com/aggregateservice/feature/catalog/presentation/screen/CatalogScreen.kt` returns at least 1 match
    - `grep -n "ProviderDetailScreen" feature/catalog/src/commonMain/kotlin/com/aggregateservice/feature/catalog/presentation/screen/CatalogScreen.kt` returns at least 1 match
    - File compiles without errors
  </acceptance_criteria>
  <verify>
    <automated>./gradlew :feature:catalog:compileKotlinAndroid 2>&1 | grep -E "(error|BUILD)"</automated>
  </verify>
  <done>CatalogScreen displays providers from API using ProviderCard</done>
</task>

<task type="auto">
  <name>Task 2: Wire SearchScreen to use ScreenModel and display results</name>
  <files>
    - feature/catalog/src/commonMain/kotlin/com/aggregateservice/feature/catalog/presentation/screen/SearchScreen.kt
  </files>
  <read_first>
    - feature/catalog/src/commonMain/kotlin/com/aggregateservice/feature/catalog/presentation/screen/SearchScreen.kt
    - feature/catalog/src/commonMain/kotlin/com/aggregateservice/feature/catalog/presentation/screenmodel/SearchScreenModel.kt
  </read_first>
  <action>
    SearchScreen already has SearchScreenModel with debounced search. It needs to:
    1. Get the ScreenModel via `koinScreenModel<SearchScreenModel>()`
    2. Display search input field bound to `screenModel.onSearchQueryChanged()`
    3. Display search results using ProviderCard component
    4. Navigate to ProviderDetailScreen on provider tap

    The SearchScreenModel has:
    - `uiState: StateFlow<SearchUiState>` with search results
    - `onSearchQueryChanged(query: String)` to update search query
    - Debounce is already implemented (300ms)

    SearchUiState likely contains `providers`, `isLoading`, `error`, `isEmpty` fields.

    Navigation to ProviderDetailScreen:
    ```kotlin
    val navigator = LocalNavigator.currentOrThrow
    navigator.push(ProviderDetailScreen(provider.id))
    ```
  </action>
  <acceptance_criteria>
    - `grep -n "koinScreenModel<SearchScreenModel>" feature/catalog/src/commonMain/kotlin/com/aggregateservice/feature/catalog/presentation/screen/SearchScreen.kt` returns at least 1 match
    - `grep -n "ProviderCard" feature/catalog/src/commonMain/kotlin/com/aggregateservice/feature/catalog/presentation/screen/SearchScreen.kt` returns at least 1 match
    - `grep -n "ProviderDetailScreen" feature/catalog/src/commonMain/kotlin/com/aggregateservice/feature/catalog/presentation/screen/SearchScreen.kt` returns at least 1 match
    - File compiles without errors
  </acceptance_criteria>
  <verify>
    <automated>./gradlew :feature:catalog:compileKotlinAndroid 2>&1 | grep -E "(error|BUILD)"</automated>
  </verify>
  <done>SearchScreen displays debounced search results with ProviderCard</done>
</task>

<task type="auto">
  <name>Task 3: Wire ProviderDetailScreen to use ScreenModel and handle navigation</name>
  <files>
    - feature/catalog/src/commonMain/kotlin/com/aggregateservice/feature/catalog/presentation/screen/ProviderDetailScreen.kt
  </files>
  <read_first>
    - feature/catalog/src/commonMain/kotlin/com/aggregateservice/feature/catalog/presentation/screen/ProviderDetailScreen.kt
    - feature/catalog/src/commonMain/kotlin/com/aggregateservice/feature/catalog/presentation/screenmodel/ProviderDetailScreenModel.kt
  </read_first>
  <action>
    ProviderDetailScreen needs to be wired to its ScreenModel (ProviderDetailScreenModel).

    ProviderDetailScreen is a data class Screen with constructor parameters:
    ```kotlin
    data class ProviderDetailScreen(val providerId: String) : Screen
    ```

    It needs to:
    1. Get the ScreenModel via `koinScreenModel<ProviderDetailScreenModel>()`
    2. Call `screenModel.initialize(providerId)` to load provider data
    3. Collect uiState and display provider details
    4. Display services list
    5. Handle favorite toggle button (uses `screenModel.onFavoriteToggle()`)
    6. Navigate to SelectServiceScreen when user selects a service or taps "Book"

    For booking navigation, inject BookingNavigator:
    ```kotlin
    val bookingNavigator: BookingNavigator = koin.get()
    // When user taps Book: navigator.push(bookingNavigator.createSelectServiceScreen(providerId, providerName))
    ```

    Import for BookingNavigator:
    `import com.aggregateservice.core.navigation.BookingNavigator`
  </action>
  <acceptance_criteria>
    - `grep -n "koinScreenModel<ProviderDetailScreenModel>" feature/catalog/src/commonMain/kotlin/com/aggregateservice/feature/catalog/presentation/screen/ProviderDetailScreen.kt` returns at least 1 match
    - `grep -n "BookingNavigator" feature/catalog/src/commonMain/kotlin/com/aggregateservice/feature/catalog/presentation/screen/ProviderDetailScreen.kt` returns at least 1 match
    - `grep -n "createSelectServiceScreen" feature/catalog/src/commonMain/kotlin/com/aggregateservice/feature/catalog/presentation/screen/ProviderDetailScreen.kt` returns at least 1 match
    - File compiles without errors
  </acceptance_criteria>
  <verify>
    <automated>./gradlew :feature:catalog:compileKotlinAndroid 2>&1 | grep -E "(error|BUILD)"</automated>
  </verify>
  <done>ProviderDetailScreen wired to ScreenModel with booking navigation</done>
</task>

<task type="auto">
  <name>Task 4: Wire CategorySelectionScreen navigation</name>
  <files>
    - feature/catalog/src/commonMain/kotlin/com/aggregateservice/feature/catalog/presentation/screen/CategorySelectionScreen.kt
  </files>
  <read_first>
    - feature/catalog/src/commonMain/kotlin/com/aggregateservice/feature/catalog/presentation/screen/CategorySelectionScreen.kt
  </read_first>
  <action>
    CategorySelectionScreen needs to be wired to navigation.

    CategorySelectionScreen likely:
    - Is a Screen that shows categories in a list
    - Allows user to select one category
    - Should navigate back with the selected category ID

    If it's meant to return a result (like a picker), it should use `navigator.pop()` with a result.

    If it's a standalone screen, it should navigate to the catalog with the selected filter.

    Check the existing implementation to understand the intended behavior:
    - If it has `onCategorySelected` callback, use `navigator.pop(result)` to return selection
    - If it's a filter screen, update the parent screen's state via shared ViewModel or SavedStateHandle

    The typical pattern for a picker is:
    ```kotlin
    onCategoryClick = { category ->
        navigator.pop(category.id)  // or navigator.pop with result
    }
    ```
  </action>
  <acceptance_criteria>
    - `grep -n "navigator" feature/catalog/src/commonMain/kotlin/com/aggregateservice/feature/catalog/presentation/screen/CategorySelectionScreen.kt` returns at least 1 match
    - File compiles without errors
  </acceptance_criteria>
  <verify>
    <automated>./gradlew :feature:catalog:compileKotlinAndroid 2>&1 | grep -E "(error|BUILD)"</automated>
  </verify>
  <done>CategorySelectionScreen navigates back with selection</done>
</task>

</tasks>

<verification>
- ./gradlew :feature:catalog:compileKotlinAndroid passes
</verification>

<success_criteria>
1. CatalogScreen displays providers from API with ProviderCard
2. SearchScreen performs debounced search and displays results
3. ProviderDetailScreen shows provider details and navigates to booking flow
4. CategorySelectionScreen returns selected category to caller
</success_criteria>

<output>
After completion, create `.planning/phases/01-ui-integration-mvp-completion/01-03-SUMMARY.md`
</output>
