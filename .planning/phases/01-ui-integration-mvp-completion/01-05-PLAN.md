---
phase: 01-ui-integration-mvp-completion
plan: "05"
type: execute
wave: 2
depends_on:
  - "01"
  - "02"
files_modified:
  - feature/favorites/src/commonMain/kotlin/com/aggregateservice/feature/favorites/presentation/screen/FavoritesScreen.kt
  - feature/reviews/src/commonMain/kotlin/com/aggregateservice/feature/reviews/presentation/screen/ReviewsScreen.kt
  - feature/reviews/src/commonMain/kotlin/com/aggregateservice/feature/reviews/presentation/screen/WriteReviewDialog.kt
  - feature/profile/src/commonMain/kotlin/com/aggregateservice/feature/profile/presentation/screen/ProfileScreen.kt
autonomous: true
requirements:
  - FAV-03
  - FAV-04
  - REV-04
  - REV-05
  - PROF-04
must_haves:
  truths:
    - "FavoritesScreen shows list of user's favorite providers"
    - "ReviewsScreen shows provider reviews"
    - "WriteReviewDialog allows submitting a review"
    - "ProfileScreen shows and allows editing user profile"
  artifacts:
    - path: "feature/favorites/src/commonMain/kotlin/com/aggregateservice/feature/favorites/presentation/screen/FavoritesScreen.kt"
      provides: "Favorites list with remove functionality"
    - path: "feature/reviews/src/commonMain/kotlin/com/aggregateservice/feature/reviews/presentation/screen/ReviewsScreen.kt"
      provides: "Reviews list with rating display"
---

<objective>
Wire FavoritesScreen, ReviewsScreen, WriteReviewDialog, and ProfileScreen to their ScreenModels and navigation.
</objective>

<context>
@feature/favorites/src/commonMain/kotlin/com/aggregateservice/feature/favorites/presentation/screen/FavoritesScreen.kt
@feature/reviews/src/commonMain/kotlin/com/aggregateservice/feature/reviews/presentation/screen/ReviewsScreen.kt
@feature/reviews/src/commonMain/kotlin/com/aggregateservice/feature/reviews/presentation/screen/WriteReviewDialog.kt
@feature/profile/src/commonMain/kotlin/com/aggregateservice/feature/profile/presentation/screen/ProfileScreen.kt

**FavoritesScreen (FAV-03, FAV-04):**
- Already has FavoritesScreenModel wired (from Plan 02)
- Navigation to ProviderDetailScreen uncommented (Plan 02)
- Needs to ensure add/remove favorite functionality works end-to-end

**ReviewsScreen (REV-04):**
- Needs to be wired to ReviewsScreenModel
- Displays list of reviews with ratings
- Should navigate to WriteReviewDialog

**WriteReviewDialog (REV-05):**
- Dialog for submitting a new review
- Should be shown as overlay/dialog from ReviewsScreen
- Needs rating (1-5 stars) and text input

**ProfileScreen (PROF-04):**
- Needs to show user profile data
- Needs edit mode for profile fields
- Uses AuthStateProvider for user data
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify FavoritesScreen favorite toggle works end-to-end</name>
  <files>
    - feature/favorites/src/commonMain/kotlin/com/aggregateservice/feature/favorites/presentation/screen/FavoritesScreen.kt
    - feature/favorites/src/commonMain/kotlin/com/aggregateservice/feature/favorites/presentation/screenmodel/FavoritesScreenModel.kt
  </files>
  <read_first>
    - feature/favorites/src/commonMain/kotlin/com/aggregateservice/feature/favorites/presentation/screen/FavoritesScreen.kt
    - feature/favorites/src/commonMain/kotlin/com/aggregateservice/feature/favorites/presentation/screenmodel/FavoritesScreenModel.kt
  </read_first>
  <action>
    FavoritesScreen was fixed in Plan 02 (navigation uncommented). Verify and ensure end-to-end favorite flow works.

    The FavoritesScreen should:
    1. Load favorites via `screenModel.loadFavorites()` on LaunchedEffect(Unit)
    2. Display favorites in LazyColumn using FavoriteCard
    3. Allow removing via `screenModel.confirmRemove(favorite)` -> `screenModel.removeFavorite()`
    4. Navigate to ProviderDetailScreen on tap (already fixed in Plan 02)

    The FavoritesScreenModel should have:
    - `loadFavorites()` - loads from GetFavoritesUseCase
    - `confirmRemove(favorite)` - shows confirmation dialog
    - `removeFavorite()` - calls RemoveFavoriteUseCase
    - `clearError()` - clears error state

    If the ScreenModel needs updating to fully implement FAV-03/FAV-04, make necessary changes.
  </action>
  <acceptance_criteria>
    - `grep -n "loadFavorites" feature/favorites/src/commonMain/kotlin/com/aggregateservice/feature/favorites/presentation/screen/FavoritesScreen.kt` returns at least 1 match
    - `grep -n "removeFavorite" feature/favorites/src/commonMain/kotlin/com/aggregateservice/feature/favorites/presentation/screen/FavoritesScreen.kt` returns at least 1 match
    - File compiles without errors
  </acceptance_criteria>
  <verify>
    <automated>./gradlew :feature:favorites:compileKotlinAndroid 2>&1 | grep -E "(error|BUILD)"</automated>
  </verify>
  <done>FavoritesScreen shows favorites list and allows removal</done>
</task>

<task type="auto">
  <name>Task 2: Wire ReviewsScreen to ScreenModel</name>
  <files>
    - feature/reviews/src/commonMain/kotlin/com/aggregateservice/feature/reviews/presentation/screen/ReviewsScreen.kt
  </files>
  <read_first>
    - feature/reviews/src/commonMain/kotlin/com/aggregateservice/feature/reviews/presentation/screen/ReviewsScreen.kt
    - feature/reviews/src/commonMain/kotlin/com/aggregateservice/feature/reviews/presentation/screenmodel/ReviewsScreenModel.kt
  </read_first>
  <action>
    Wire ReviewsScreen to its ScreenModel.

    ReviewsScreen should:
    1. Get `ReviewsScreenModel` via `koinScreenModel<ReviewsScreenModel>()`
    2. Get providerId from constructor (data class screen: `data class ReviewsScreen(val providerId: String) : Screen`)
    3. Call `screenModel.initialize(providerId)` to load reviews
    4. Display reviews in LazyColumn with rating stars
    5. Have "Write Review" button that shows WriteReviewDialog

    Navigation to WriteReviewDialog:
    ```kotlin
    // Show dialog when user taps "Write Review"
    var showWriteReviewDialog by remember { mutableStateOf(false) }

    if (showWriteReviewDialog) {
        WriteReviewDialog(
            providerId = providerId,
            onDismiss = { showWriteReviewDialog = false },
            onSubmit = { rating, comment ->
                screenModel.submitReview(rating, comment)
                showWriteReviewDialog = false
            }
        )
    }
    ```
  </action>
  <acceptance_criteria>
    - `grep -n "koinScreenModel<ReviewsScreenModel>" feature/reviews/src/commonMain/kotlin/com/aggregateservice/feature/reviews/presentation/screen/ReviewsScreen.kt` returns at least 1 match
    - `grep -n "WriteReviewDialog" feature/reviews/src/commonMain/kotlin/com/aggregateservice/feature/reviews/presentation/screen/ReviewsScreen.kt` returns at least 1 match
    - File compiles without errors
  </acceptance_criteria>
  <verify>
    <automated>./gradlew :feature:reviews:compileKotlinAndroid 2>&1 | grep -E "(error|BUILD)"</automated>
  </verify>
  <done>ReviewsScreen displays reviews and allows writing new ones</done>
</task>

<task type="auto">
  <name>Task 3: Wire WriteReviewDialog to ScreenModel</name>
  <files>
    - feature/reviews/src/commonMain/kotlin/com/aggregateservice/feature/reviews/presentation/screen/WriteReviewDialog.kt
  </files>
  <read_first>
    - feature/reviews/src/commonMain/kotlin/com/aggregateservice/feature/reviews/presentation/screen/WriteReviewDialog.kt
    - feature/reviews/src/commonMain/kotlin/com/aggregateservice/feature/reviews/presentation/screenmodel/ReviewsScreenModel.kt
  </read_first>
  <action>
    Wire WriteReviewDialog for review submission.

    WriteReviewDialog should be a composable Dialog that:
    1. Accepts `providerId: String`, `onDismiss: () -> Unit`, `onSubmit: (rating: Int, comment: String) -> Unit`
    2. Has a 5-star rating selector (tappable stars)
    3. Has a text field for review comment
    4. Has Cancel and Submit buttons

    The submit should call `onSubmit(rating, comment)` which the parent (ReviewsScreen) handles by calling `screenModel.submitReview(rating, comment)`.

    Star rating UI example:
    ```kotlin
    Row {
        (1..5).forEach { star ->
            Text(
                text = if (star <= selectedRating) "★" else "☆",
                style = MaterialTheme.typography.headlineMedium,
                color = if (star <= selectedRating) Color.Yellow else Color.Gray,
                modifier = Modifier.clickable { selectedRating = star }
            )
        }
    }
    ```

    Import Dialog from:
    `import androidx.compose.ui.window.Dialog`
  </action>
  <acceptance_criteria>
    - `grep -n "Dialog" feature/reviews/src/commonMain/kotlin/com/aggregateservice/feature/reviews/presentation/screen/WriteReviewDialog.kt` returns at least 1 match
    - `grep -n "rating" feature/reviews/src/commonMain/kotlin/com/aggregateservice/feature/reviews/presentation/screen/WriteReviewDialog.kt` returns at least 1 match (case insensitive)
    - File compiles without errors
  </acceptance_criteria>
  <verify>
    <automated>./gradlew :feature:reviews:compileKotlinAndroid 2>&1 | grep -E "(error|BUILD)"</automated>
  </verify>
  <done>WriteReviewDialog allows selecting rating and writing comment</done>
</task>

<task type="auto">
  <name>Task 4: Wire ProfileScreen to ScreenModel</name>
  <files>
    - feature/profile/src/commonMain/kotlin/com/aggregateservice/feature/profile/presentation/screen/ProfileScreen.kt
  </files>
  <read_first>
    - feature/profile/src/commonMain/kotlin/com/aggregateservice/feature/profile/presentation/screen/ProfileScreen.kt
    - feature/profile/src/commonMain/kotlin/com/aggregateservice/feature/profile/presentation/screenmodel/ProfileScreenModel.kt
  </read_first>
  <action>
    Wire ProfileScreen to its ScreenModel with edit mode.

    ProfileScreen should:
    1. Get `ProfileScreenModel` via `koinScreenModel<ProfileScreenModel>()`
    2. Get userId from `AuthStateProvider.currentUserId`
    3. Call `screenModel.initialize(userId)` to load profile
    4. Display profile fields (name, email, phone, avatar)
    5. Have Edit button to toggle edit mode
    6. In edit mode, show text fields for editable fields
    7. Save button calls `screenModel.saveProfile()`

    The screen should handle two states:
    - View mode: display profile data
    - Edit mode: show editable TextFields

    Use `uiState.isEditing` or similar flag to toggle between modes.

    ProfileUiState likely has fields like:
    - `user: User?` (profile data)
    - `isLoading: Boolean`
    - `isEditing: Boolean` (edit mode toggle)
    - `error: AppError?`
  </action>
  <acceptance_criteria>
    - `grep -n "koinScreenModel<ProfileScreenModel>" feature/profile/src/commonMain/kotlin/com/aggregateservice/feature/profile/presentation/screen/ProfileScreen.kt` returns at least 1 match
    - `grep -n "AuthStateProvider" feature/profile/src/commonMain/kotlin/com/aggregateservice/feature/profile/presentation/screen/ProfileScreen.kt` returns at least 1 match
    - File compiles without errors
  </acceptance_criteria>
  <verify>
    <automated>./gradlew :feature:profile:compileKotlinAndroid 2>&1 | grep -E "(error|BUILD)"</automated>
  </verify>
  <done>ProfileScreen shows user profile with edit mode</done>
</task>

</tasks>

<verification>
- ./gradlew :feature:favorites:compileKotlinAndroid passes
- ./gradlew :feature:reviews:compileKotlinAndroid passes
- ./gradlew :feature:profile:compileKotlinAndroid passes
</verification>

<success_criteria>
1. FavoritesScreen shows favorites and allows removing them
2. ReviewsScreen shows provider reviews and allows writing new ones
3. WriteReviewDialog provides rating selection and comment input
4. ProfileScreen shows user data with working edit mode
</success_criteria>

<output>
After completion, create `.planning/phases/01-ui-integration-mvp-completion/01-05-SUMMARY.md`
</output>
